Local font files (if any) go here.

Fonts are currently loaded from Google Fonts via @import in css/global.css:
  Playfair Display, Cormorant Garamond, Montserrat, Cairo

To self-host instead, add the .woff2 files here and define @font-face rules
in css/global.css, then remove the Google Fonts @import.
